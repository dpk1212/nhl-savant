# Copy to .env (never commit .env). Scripts load via dotenv.

# The Odds API — multi-book odds (retail + fair cascade)
ODDS_API_KEY=

# pinnapi.com — Pinnacle-native odds + max stake for snapshotPinnacle.js
# Free trial ≈ 100 req/day (prove only). Production needs REST $99/mo+.
# Edge ($149) = REST + SSE drops; snapshot also polls GET /api/drops each cycle.
# Also add as GitHub Actions secret PINNAPI_KEY for fetch-polymarket.yml.
PINNAPI_KEY=
