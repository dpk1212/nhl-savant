name: Generate Expert Analysis

on:
  schedule:
    # Run at 9 AM ET (2 PM UTC) daily - generates matchup insights
    - cron: '0 14 * * *'
    # Run at 2 PM ET (7 PM UTC) daily - generates bet narratives after odds available
    - cron: '0 19 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  generate-analysis:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate expert analysis
        env:
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID }}
          FIREBASE_CLIENT_EMAIL: ${{ secrets.FIREBASE_CLIENT_EMAIL }}
          FIREBASE_PRIVATE_KEY: ${{ secrets.FIREBASE_PRIVATE_KEY }}
          PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
        run: node scripts/generateExpertAnalysis.js

      - name: Notify completion
        if: success()
        run: |
          echo "✅ Expert analysis generated successfully"
          echo "📰 Matchup insights cached in Firebase"
          echo "🎯 Bet narratives cached (if odds data available)"

      - name: Notify failure
        if: failure()
        run: |
          echo "❌ Expert analysis generation failed"
          echo "Check logs for details"

