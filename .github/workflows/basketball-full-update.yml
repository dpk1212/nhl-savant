name: Basketball Full Update (Grade + ROI + Confidence)

on:
  workflow_dispatch:  # Manual trigger anytime
    inputs:
      skip_grading:
        description: 'Skip grading (just update ROI/confidence)'
        required: false
        default: 'false'
        type: boolean

jobs:
  full-update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm ci
      
      # Create .env file from secrets (scripts use dotenv to load this)
      - name: Create .env file
        run: |
          echo "VITE_FIREBASE_API_KEY=${{ secrets.VITE_FIREBASE_API_KEY }}" >> .env
          echo "VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}" >> .env
          echo "VITE_FIREBASE_PROJECT_ID=${{ secrets.VITE_FIREBASE_PROJECT_ID }}" >> .env
          echo "VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}" >> .env
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}" >> .env
          echo "VITE_FIREBASE_APP_ID=${{ secrets.VITE_FIREBASE_APP_ID }}" >> .env
          echo "FIREBASE_CLIENT_EMAIL=${{ secrets.FIREBASE_CLIENT_EMAIL }}" >> .env
          echo "FIREBASE_PRIVATE_KEY=${{ secrets.FIREBASE_PRIVATE_KEY }}" >> .env
          echo "🔍 Checking which secrets are set..."
          [ -n "${{ secrets.VITE_FIREBASE_API_KEY }}" ] && echo "✅ VITE_FIREBASE_API_KEY is set" || echo "❌ VITE_FIREBASE_API_KEY is MISSING"
          [ -n "${{ secrets.VITE_FIREBASE_PROJECT_ID }}" ] && echo "✅ VITE_FIREBASE_PROJECT_ID is set" || echo "❌ VITE_FIREBASE_PROJECT_ID is MISSING"
      
      # Step 1: Grade bets (optional)
      - name: Grade Basketball Bets
        if: ${{ inputs.skip_grading != 'true' }}
        run: |
          echo "🏀 Step 1: Grading basketball bets..."
          npm run grade-basketball-bets
          echo "✅ Grading complete!"
      
      # Step 2: Update Pattern ROI
      - name: Update Pattern ROI
        run: |
          echo "📊 Step 2: Updating pattern ROI..."
          npm run update-pattern-roi
          echo "✅ Pattern ROI updated!"
      
      # Step 3: Update Dynamic Confidence Weights
      - name: Update Dynamic Confidence Weights
        run: |
          echo "🎯 Step 3: Updating dynamic confidence weights..."
          npm run update-dynamic-confidence
          echo "✅ Confidence weights updated!"
      
      # Step 4: Update Pending Bets with new data
      - name: Update Pending Bets
        run: |
          echo "📝 Step 4: Updating pending bets..."
          npm run update-pending-bets
          echo "✅ Pending bets updated!"
      
      # Step 5: Analyze Spread Performance (legacy)
      - name: Analyze Spread Performance
        run: |
          echo "🎯 Step 5: Analyzing spread opportunity performance..."
          npm run analyze-spread-performance
          echo "✅ Spread analysis complete!"
      
      # Step 6: Prime Picks Performance Analysis
      - name: Prime Picks Performance
        run: |
          echo "🌟 Step 6: Prime Picks performance breakdown..."
          npm run analyze-prime-performance
          echo "✅ Prime Picks performance analysis complete!"
      
      # Step 7: Prime Picks Unit Sizing Analysis
      - name: Prime Picks Unit Sizing
        run: |
          echo "📊 Step 7: Unit sizing analysis (EV%, MOS, conviction tiers)..."
          npm run analyze-prime-unit-sizing
          echo "✅ Unit sizing analysis complete!"
      
      # Step 8: Historical Spread Edge (ATS) Analysis
      - name: Spread Edge ATS Analysis
        run: |
          echo "🎯 Step 8: Historical spread edge (ATS) analysis..."
          npm run analyze-spread-edge
          echo "✅ Spread edge analysis complete!"
      
      # Step 9: Commit and push weights + spread analysis
      - name: Commit updated data
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add public/basketball_confidence_weights.json public/spread_performance.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "🎯 Full basketball update [$(date +'%Y-%m-%d %H:%M')]" && git push)
      
      # Step 10: Build and deploy
      - name: Build site
        run: npm run build
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'
      
      - name: Summary
        run: |
          echo "🎉 BASKETBALL FULL UPDATE COMPLETE!"
          echo ""
          echo "Steps completed:"
          echo "  ✅ 1. Grade bets (if enabled)"
          echo "  ✅ 2. Update pattern ROI"
          echo "  ✅ 3. Update dynamic confidence weights"
          echo "  ✅ 4. Update pending bets"
          echo "  ✅ 5. Analyze spread performance"
          echo "  ✅ 6. Prime Picks performance (W-L, ROI by segment)"
          echo "  ✅ 7. Unit sizing analysis (EV%, MOS, conviction tiers)"
          echo "  ✅ 8. Spread edge ATS analysis (cover rate, filters)"
          echo "  ✅ 9. Deploy to GitHub Pages"
